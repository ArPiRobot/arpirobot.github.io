image: python:3.8-buster

before_script:
  - pip install mkdocs markdown-fenced-code-tabs

pages:
  script:
  - mkdocs build
  - mv site public
  artifacts:
    paths:
    - public
  only:
  - master
